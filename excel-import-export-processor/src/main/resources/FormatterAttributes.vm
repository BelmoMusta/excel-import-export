#foreach($formatter_ in $wrapper.formatters)
    #if(!$config.useFQNs)
        #set($formatterClassName=${formatter_.simpleName})
    #else
        #set($formatterClassName=${formatter_.fullFQN})
    #end
    #if($wrapper.withIoC)
        $config.autowiredAnnotation
    private $config.formatterClassName $formatter_.instanceName;
    #else
    private final $config.formatterClassName $formatter_.instanceName = new ${formatterClassName}();
    #end
#end